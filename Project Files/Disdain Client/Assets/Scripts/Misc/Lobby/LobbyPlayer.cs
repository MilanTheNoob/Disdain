using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LobbyPlayer : MonoBehaviour
{
    void Start()
    {
        
    }

    void FixedUpdate()
    {
        /*
        if (controller.enabled)
            controller.enabled = false;

        if (transform.position == oldPos) { animationTick += 1; } else { animationTick = 0; }
        if (animationTick >= 5) { anim.SetFloat("MovementValue", 0f); } else { anim.SetFloat("MovementValue", 1f); }
        oldPos = transform.position;
        */
        //ClientSend.SendPlayerMovement(GameManager.Horizontal, GameManager.Vertical,
            //GameManager.instance.MobileInput.JumpB.);
    }
}
